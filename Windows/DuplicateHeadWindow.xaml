<ctrl:FrostyDockableWindow x:Class="BundleRefTablePlugin.Windows.DuplicateHeadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        mc:Ignorable="d"
        Title="Duplicate Head" Height="200" Width="500" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10" Background="{StaticResource ListBackground}">
            <DockPanel Margin="5" LastChildFill="True">
                <Label Content="Source" FontFamily="Global User Interface" Width="80"/>
                <TextBox x:Name="sourceFolderTextBox" Padding="4" FontFamily="Global User Interface" IsEnabled="False" BorderThickness="1"/>
            </DockPanel>
            <DockPanel Margin="5" LastChildFill="True">
                <Label Content="New Folder" FontFamily="Global User Interface" Width="80"/>
                <TextBox x:Name="newFolderTextBox" Padding="4" FontFamily="Global User Interface" BorderThickness="1"/>
            </DockPanel>
            <DockPanel Margin="5" LastChildFill="True">
                <Label Content="Host" FontFamily="Global User Interface" Width="80"/>
                <TextBox x:Name="hostFolderTextBox" Padding="4" FontFamily="Global User Interface" BorderThickness="1"
                         ToolTip="Existing player folder whose bundle will host the new assets in the BRT (leave empty to use source)"/>
            </DockPanel>
        </StackPanel>

        <Border Grid.Row="1">
            <Grid Margin="8">
                <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Left" Width="75" Click="CancelButton_Click"/>
                <Button x:Name="duplicateButton" Content="Duplicate" HorizontalAlignment="Right" Width="100" Click="DuplicateButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
